                {/* Main node bubble */}
                <circle
                  r={(node.level === 0 ? 35 : 26) * baseScale}
                  fill={fillColor}
                  stroke={strokeColor}
                  strokeWidth={(isCurrent ? 3 : isPath ? 2 : isPlanned ? 1.5 : 1) * baseScale}
                  filter={isCurrent ? "url(#neon-glow-strong)" : undefined}
                  className={`transition-all duration-700 group-hover:scale-110 ${isPlanned && !isPath ? "animate-pulse" : ""}`}
                />
